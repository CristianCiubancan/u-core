{
  "name": "qb-core-port-wip",
  "version": "1.3.0",
  "fx_version": "cerulean",
  "games": ["gta5"],
  "lua54": true,
  "author": "u-core (TS port of qbcore-framework/qb-core)",
  "description": "WIP port of qb-core. Phase 2a: Shared data wiring + GetCoreObject exports. The `-port-wip` suffix keeps this from claiming the `qb-core` resource name while upstream is still authoritative; we rename to plain `qb-core` and patch the txAdmin recipe once Phase 2b/c land the runtime code.",
  "server_scripts": ["server/*.js"],
  "dependencies": ["oxmysql"]
}
