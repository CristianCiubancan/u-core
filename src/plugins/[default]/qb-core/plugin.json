{
  "name": "qb-core-port-wip",
  "version": "1.3.0",
  "fx_version": "cerulean",
  "games": ["gta5"],
  "lua54": true,
  "author": "u-core (TS port of qbcore-framework/qb-core)",
  "description": "WIP port of qb-core. Phase 1 (shared data) only — no exports wired yet. The `-port-wip` suffix keeps this from claiming the `qb-core` resource name while upstream is still authoritative; we rename to plain `qb-core` and patch the txAdmin recipe once Phase 2/3 land the runtime code.",
  "dependencies": ["oxmysql"]
}
